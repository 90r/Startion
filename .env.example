# GitHub personal access token (needs `read:user` scope, or `repo` for private stars)
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
# Leave empty to use the authenticated user's stars; set to sync another user's public stars
GITHUB_USERNAME=

# Notion integration token — create at https://www.notion.so/profile/integrations/internal
NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxx
# Data source ID — run `uv run startion setup` to create one, or copy from Notion database settings
NOTION_DATA_SOURCE_ID=

# OpenAI-compatible API (works with OpenAI, Azure, Ollama, vLLM, etc.)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Summary language: English / Chinese (default: English)
SUMMARY_LANGUAGE=English

# Max concurrent AI summary requests (default: 5)
SYNC_CONCURRENCY=5
